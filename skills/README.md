# Skills – kickstart-nuxt-ssr

Read [`AGENTS.md`](../AGENTS.md) first: it indexes every skill folder, path, and what it covers. Each `skills/<name>/` directory contains `SKILL.md` with YAML frontmatter (`name`, `description`).
